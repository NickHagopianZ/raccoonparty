extends BaseEntity


func focus(by_entity: Node) -> void:
	# NPC focused by an entity (e.g., player)
	# TODO make the entity look at the NPC or show some UI indicator
	pass

func interact(by_entity: Node) -> void:
	# NPC interacted with by an entity (e.g., player)
	pass